/*
 * Reconstructed level3 (Rainfall).
 * Vulnerability: printf(buffer) → format string. Global m must be 64 for system("/bin/sh").
 */

#include <stdio.h>

int m;  /* global @ 0x804988c */

void v(void)
{
	char buf[512];

	fgets(buf, 512, stdin);
	printf(buf);  /* format string: user-controlled input */
	if (m == 0x40) { /* 64 */
		fwrite("Wait what?!\n", 1, 12, stdout);
		system("/bin/sh");
	}
}

int main(void)
{
	v();
	return 0;
}
