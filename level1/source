/*
 * Reconstructed level1 (Rainfall).
 * Vulnerability: unbounded gets() → overflow → redirect to run().
 */

#include <stdio.h>
#include <stdlib.h>

void run(void)
{
	fwrite("Good... Wait what?\n", 1, 19, stdout);
	system("/bin/sh");
}

int main(void)
{
	char buf[76];  /* at esp+0x10, frame 0x50 → ret at +68 */

	gets(buf);
	return 0;
}
