/*
 * Level5 reconstruction (Rainfall).
 * n(): printf(buffer) then exit(1). o(): system("/bin/sh"), never called.
 * Exploit: overwrite exit GOT with address of o.
 */

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

void o(void)
{
	system("/bin/sh");
	_exit(1);
}

void n(void)
{
	char buf[512];

	fgets(buf, 512, stdin);
	printf(buf);  /* format string */
	exit(1);      /* GOT at 0x8049838 → replace with &o */
}

int main(void)
{
	n();
	return 0;
}
